# Generated by generate_dockerfiles.py

FROM aorwall/swe-bench-conda:bookworm-slim

USER root
WORKDIR /opt


RUN git clone https://github.com/relekang/python-semantic-release.git

WORKDIR /opt/python-semantic-release

RUN git -c advice.detachedHead=false checkout 3689157c2

RUN pip install --no-cache-dir -e .

RUN pip install --no-cache-dir pytest pytest-cov coverage

RUN git checkout 3689157c2 .

ENV TESTBED_NAME=relekang__python-semantic-release__3689157c2
ENV IMAGE_TYPE=python
ENV REPO_DIR=/opt/python-semantic-release

WORKDIR /opt

COPY swebench_docker swebench_docker
COPY --chmod=0755 docker/entrypoint.sh .

ENTRYPOINT ["./entrypoint.sh"]
