# Generated by generate_dockerfiles.py

FROM aorwall/swe-bench-conda:bookworm-slim

USER root
WORKDIR /opt


RUN git clone https://github.com/httpie/httpie.git

WORKDIR /opt/httpie

RUN git -c advice.detachedHead=false checkout bb36897

RUN pip install --no-cache-dir -e .

RUN pip install --no-cache-dir pytest pytest-cov coverage

RUN git checkout bb36897 .

ENV TESTBED_NAME=httpie__httpie__bb36897
ENV IMAGE_TYPE=python
ENV REPO_DIR=/opt/httpie

WORKDIR /opt

COPY swebench_docker swebench_docker
COPY --chmod=0755 docker/entrypoint.sh .

ENTRYPOINT ["./entrypoint.sh"]
